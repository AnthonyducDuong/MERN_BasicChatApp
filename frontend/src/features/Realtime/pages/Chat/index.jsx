import React from 'react';
import PropTypes from 'prop-types';

Chat.propTypes = {

};

function Chat(props) {
    return (
        <div>
            Chat nè
        </div>
    );
}

export default Chat;